description: >
  Gracefully stop a TestingBot Tunnel previously started with start-tunnel.
  Runs even when earlier steps failed, so the tunnel is always cleaned up.

steps:
  - run:
      name: Stop TestingBot Tunnel
      when: always
      command: << include(scripts/stop-tunnel.sh) >>
